# Errors

todo topics to cover:

- Error variants and when each occurs (Compile, Instantiate, Config, MissingMemory, MissingAllocator, MissingExport, ExportTypeMismatch, Trap, LimitExceeded, GuestExit, Poisoned, Memory, AllocationFailed, ValueTooLarge).
- MemoryError for invalid guest pointers (NegativePointer, NegativeLength, RangeOverflow, OutOfBounds, LengthMismatch).
- GuestError at the top level (Runtime vs UnsupportedAbi).
- Limit variants (Epoch, Fuel, Stack).
